# GateTools 配置文件
# 作者: NSrank, Augment
# 版本: 1.1

# 基础设置
settings:
  # 最大传送区域数量限制
  max-gates: 50

  # 删除确认超时时间（秒）
  delete-confirm-timeout: 30

  # 传送缓冲时间（tick，20tick = 1秒）
  teleport-delay: 60

  # 自动保存间隔（分钟）
  auto-save-interval: 5

  # 是否启用调试模式
  debug: false
